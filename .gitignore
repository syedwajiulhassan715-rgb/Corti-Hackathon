node_modules
.env
.cache/
dist
fixtures/audio/*.wav

# Provided hackathon audio — large binaries, kept out of git
fixtures/provided/audio/*
!fixtures/provided/audio/.gitkeep
fixtures/audio/*.m4a

# Stage runner output — regenerated from fixtures and the cache
runs/
