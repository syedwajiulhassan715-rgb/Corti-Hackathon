// The contract surface. Import types from here, not from the module that
// happens to use them.

export type {
  Event,
  EventId,
  EventInput,
  Millis,
  Source,
  Speaker,
} from "./events.ts";
